---
description:
globs:
alwaysApply: false
---
# 订单数据模型规范

## 核心原则
前端的数据模型必须与后端的实体类和VO类保持严格一致。

## 订单状态枚举规范

### 后端状态定义
参考：[OrderStatusEnum.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/order/enums/OrderStatusEnum.java)

**已确认的状态值：**
```dart
awaitingPayment          // 待付款
awaitingSubmission       // 等待提交要求  
buyAwaitingSubmission    // 买家材料重传
awaitingStart           // 等待开始
awaitingDelivery        // 等待卖家交付
awaitingConfirmation    // 交付待确认
sellerSupplementaryMaterials  // 卖家重新交付
applyForRefuse          // 申请拒绝
afterSale               // 售后中
AfterSaleRejection      // 售后拒绝
applyingForMediation    // 平台介入（⚠️目前禁用）
awaitingEvaluation      // 等待评价
orderCompleted          // 订单成功结束
canceled                // 已取消
```

### 前端实现位置
- 状态枚举：[order_status.dart](mdc:lib/features/orders/domain/entities/order_status.dart)
- 确保`fromString`和`toJsonString`方法与后端完全对应

## 订单需求类型规范

### 后端需求类型定义
参考：[OrderDemandService.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/orderDemand/OrderDemandService.java)

**已确认的需求类型：**
```dart
replenish_materials  // 卖家申请补充材料
refuse              // 卖家拒绝接单  
replenishment       // 买家申请补充
reform              // 买家申请重做
platform            // 平台介入（⚠️目前禁用）
```

### 需求状态枚举
参考：[OrderDemandStatusEnum.java](mdc:duoshaokk_api/xunman-common/src/main/java/vip/xunman/common/enums/xunman/OrderDemandStatusEnum.java)

```dart
WAIT     // 待审核
SUCCESS  // 审核通过  
FAIL     // 拒绝
CANCEL   // 取消
```

## 数据字段映射规范

### 1. 订单基础信息
参考后端实体：[Order.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/order/Order.java)

**关键字段映射：**
- `id` → `id` (Long)
- `orderSn` → `orderSn` (String) 
- `buyerId` → `buyerId` (Long)
- `tenantId` → `tenantId` (Long)
- `state` → `state` (OrderStatusEnum)
- `totalPrice` → `totalPrice` (BigDecimal)
- `payPrice` → `payPrice` (BigDecimal)
- `payTime` → `payTime` (DateTime)
- `deliveryTime` → `deliveryTime` (DateTime)
- `completeTime` → `completeTime` (DateTime)

### 2. 订单需求信息
参考后端实体：[OrderDemand.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/orderDemand/OrderDemand.java)

**关键字段映射：**
- `id` → `id` (Long)
- `orderId` → `orderId` (Long)
- `type` → `type` (String)
- `reasonValue` → `reasonValue` (String)
- `reasonLabel` → `reasonLabel` (String)
- `remarks` → `remarks` (String)
- `status` → `status` (OrderDemandStatusEnum)
- `memberType` → `memberType` (RefundMemberTypeEnum)

### 3. API响应格式
所有API响应都遵循统一格式：
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {...}
}
```

## 前端实现检查点

### 1. 数据模型文件检查
- [Order](mdc:lib/features/orders/domain/entities/order.dart)
- [OrderModel](mdc:lib/features/orders/data/models/order_model.dart)
- [OrderStatus](mdc:lib/features/orders/domain/entities/order_status.dart)

### 2. API数据源检查
- [IOrderRemoteDataSource](mdc:lib/features/orders/data/datasources/i_order_remote_data_source.dart)
- [OrderRemoteDataSourceImpl](mdc:lib/features/orders/data/datasources/order_remote_data_source_impl.dart)

### 3. 状态映射检查
确保以下映射正确：
- 列表页面的状态筛选
- 详情页面的状态显示
- 操作按钮的状态判断

## 强制要求

### ✅ 必须做的事情
1. 任何新增的数据字段都必须先在后端确认
2. 状态转换逻辑必须与后端业务逻辑一致
3. 错误处理必须解析后端返回的错误码和消息
4. 时间格式必须与后端一致（ISO 8601格式）

### ❌ 禁止做的事情
1. 不要创建后端不存在的状态或字段
2. 不要假设API返回的数据格式
3. 不要硬编码状态值，必须使用枚举
4. 不要忽略后端的验证规则和约束条件

## 新功能开发指南

### 开发订单需求相关功能时
1. 参考后端OrderDemand相关类
2. 确保前端需求类型与后端type字段对应
3. 实现状态跟踪时使用后端定义的状态枚举
4. API调用参数必须符合后端验证规则

### 开发售后功能时  
1. 参考后端OrderRefund相关类
2. 注意退款类型枚举的对应关系
3. 确保退款状态与订单状态的关联正确
