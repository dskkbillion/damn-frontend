---
description:
globs:
alwaysApply: false
---
# 后端API合规性规则

## 重要提醒
在开发任何前端功能时，必须确保与后端API的完全一致性和合法性。

## API端点验证规则

### 1. 订单相关API端点
参考后端控制器：[OrderApiController.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/order/OrderApiController.java)

**已确认的API端点：**
- `POST /api/shop/order/create` - 创建订单
- `POST /api/shop/order/list` - 订单列表
- `GET /api/shop/order/detail` - 订单详情
- `GET /api/shop/order/cancel` - 取消订单
- `GET /api/shop/order/complete` - 确认收货
- `POST /api/shop/order/verify` - 确认接单
- `POST /api/shop/order/ship` - 发货
- `GET /api/shop/order/remind` - 提醒发货
- `POST /api/shop/order/delete` - 删除订单
- `POST /api/shop/order/sellerDelete` - 卖家删除记录
- `POST /api/shop/order/invite` - 邀请评价

### 2. 订单需求API端点
参考后端控制器：[OrderDemandApiController.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/orderDemand/OrderDemandApiController.java)

**已确认的API端点：**
- `POST /api/project/orderDemand/add` - 申请上报
- `POST /api/project/orderDemand/audit` - 审核申请
- `POST /api/project/orderDemand/buyerList` - 买家申请列表
- `POST /api/project/orderDemand/list` - 查询申请列表
- `GET /api/project/orderDemand/get` - 查看详情

### 3. 售后相关API端点
参考后端控制器：[OrderRefundApiController.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/orderRefund/OrderRefundApiController.java)

**已确认的API端点：**
- `POST /api/shop/order-refund/apply` - 申请售后
- `POST /api/shop/order-refund/audit` - 审核售后
- `POST /api/shop/order-refund/list` - 买家售后列表
- `POST /api/shop/order-refund/listTenant` - 卖家售后列表

### 4. 评价相关API端点
参考后端控制器：[MemberEvaluateApiController.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/memberEvaluate/MemberEvaluateApiController.java)

**已确认的API端点：**
- `POST /api/shop/evaluate/add` - 添加评价
- `POST /api/shop/evaluate/list` - 评价列表

## API参数验证规则

### 1. 确保请求参数格式正确
- 参考后端DTO类型：[OrderCreateDTO](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/order/dto/OrderCreateDTO.java)
- 确保前端发送的JSON字段名与后端期望的完全一致
- 注意数据类型：Long/Integer/String/Boolean等

### 2. 查询参数验证
- 参考：[OrderQuery.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/order/OrderQuery.java)
- 分页参数：pageNum, pageSize
- 状态参数：states（数组格式）
- 角色参数：type ("buyer" 或 "seller")

## 禁止事项

### 1. 不要使用未确认的API端点
- 任何新的API调用都必须先在后端代码中确认存在
- 不要假设API端点的存在，必须验证

### 2. 正确理解已实现的功能
- **邀请评价功能已完整实现**：后端API `/api/shop/order/invite` 功能完整，包含重复邀请检查和消息通知
- **平台介入功能设计正确**：不直接改变订单状态，而是创建申请记录并设置平台标记(buyerPlatformFlag/sellerPlatformFlag)，由管理后台处理
- 参考：[OrderService.java platform和invite方法](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/order/OrderService.java)

### 3. 不要修改已确认的枚举值
- 订单状态枚举必须与后端完全一致
- 参考：[OrderStatusEnum.java](mdc:duoshaokk_api/xunman-shop/src/main/java/vip/xunman/shop/order/order/enums/OrderStatusEnum.java)

## 开发检查清单

在提交代码前，确保：
- [ ] API端点路径与后端完全一致
- [ ] 请求参数字段名和类型正确
- [ ] 响应数据解析与后端返回格式匹配
- [ ] 错误处理符合后端错误响应格式
- [ ] 状态枚举值与后端定义一致
- [ ] 没有使用未实现或被禁用的功能
