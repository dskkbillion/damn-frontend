# 聊天模块重构测试指南

## 🚀 快速开始测试

### 方法1：通过代码直接访问新聊天页面

1. **修改现有的聊天入口**
   
   找到任意导航到聊天的地方，例如消息列表页面，将路由从：
   ```dart
   context.push('/chat/$chatId');
   ```
   改为：
   ```dart
   context.push('/chat/refactored/$chatId');  // 使用新版本
   ```

2. **在主页添加测试按钮**
   
   在 `lib/features/home/presentation/pages/home_page.dart` 或任意页面添加：
   ```dart
   ElevatedButton(
     onPressed: () {
       // 使用测试聊天ID
       context.push('/chat/refactored/test_chat_001');
     },
     child: Text('测试新聊天'),
   ),
   ```

### 方法2：通过消息列表进入

1. 打开应用
2. 进入"消息"标签页
3. 点击任意聊天对话
4. 系统会自动使用新的聊天实现（如果路由已更新）

### 方法3：手动测试各项功能

## 📝 测试清单

### 基础功能测试

- [ ] **文本消息**
  - 发送普通文本
  - 发送长文本（超过100字）
  - 发送表情符号
  - 发送链接

- [ ] **图片消息**
  - 从相册选择图片
  - 拍照发送
  - 查看图片预览
  - 图片缩放

- [ ] **音频消息**
  - 录制语音
  - 播放语音
  - 暂停/继续播放

- [ ] **自定义消息（allocate）**
  - 查看商品卡片
  - 点击跳转商品详情

### 高级功能测试

- [ ] **消息操作**
  - 长按消息撤回（2分钟内）
  - 长按消息删除
  - 消息复制

- [ ] **离线功能**
  - 断网后发送消息（进入队列）
  - 恢复网络后自动发送
  - 消息缓存加载

- [ ] **WebSocket测试**
  - 接收实时消息
  - 断线自动重连
  - 连接状态显示

- [ ] **性能测试**
  - 加载历史消息（上拉加载更多）
  - 快速滚动性能
  - 大量消息渲染（100+条）

## 🔍 对比测试

### 新旧版本对比

| 测试项 | 旧版路由 | 新版路由 |
|--------|---------|----------|
| 路由格式 | `/chat/:chatId` | `/chat/refactored/:chatId` |
| 测试聊天1 | `/chat/test_001` | `/chat/refactored/test_001` |
| 测试聊天2 | `/chat/test_002` | `/chat/refactored/test_002` |

### 性能对比点

1. **启动速度**：进入聊天页面的加载时间
2. **滚动流畅度**：大量消息时的滚动性能
3. **内存占用**：查看Flutter DevTools中的内存使用
4. **消息发送延迟**：从点击发送到显示的时间

## 🐛 已知问题

1. **build_runner错误**：可以暂时忽略，不影响运行
2. **Drift数据库表**：如果遇到数据库错误，可以先注释掉本地缓存功能

## 💡 临时解决方案

如果遇到编译错误，可以：

1. **跳过本地缓存**：在 `ChatRepositoryImpl` 中暂时注释掉缓存相关代码
2. **使用内存缓存**：用 Map 代替数据库存储
3. **直接运行**：`flutter run` 忽略分析警告

## 📱 测试环境

- **开发模式**：`flutter run -t lib/main_domestic_dev.dart`
- **生产模式**：`flutter run -t lib/main_domestic.dart`
- **国际版**：`flutter run -t lib/main_international_dev.dart`

## 🎯 重点测试场景

1. **买家购买商品聊天**
   - 发送商品咨询
   - 接收卖家回复
   - 查看商品详情卡片

2. **售后服务聊天**
   - 发送问题描述
   - 上传问题图片
   - 语音说明问题

3. **客服聊天**
   - 多轮对话
   - 图文混合消息
   - 长时间对话的性能

## ✅ 测试通过标准

- 所有基础功能正常工作
- 无明显性能问题
- 无崩溃或异常退出
- 用户体验与原版一致或更好

---

**提示**：如果需要查看日志，可以使用 `flutter logs` 命令实时查看运行日志。