name: dskk_flutter_refactor
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0' # Updated to a recent SDK range

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Often needed for internationalization
    sdk: flutter
  cupertino_icons: ^1.0.6 # Keep default icons

  # Core Libraries - Tech Stack Selection
  flutter_riverpod: ^2.5.1 # State Management (Riverpod) - Re-enabled
  riverpod_annotation: ^2.3.5 # Riverpod Annotations for generator - Re-enabled
  get_it: ^7.7.0 # Dependency Injection (Service Locator)
  injectable: ^2.4.1 # DI Code Generator Helper
  go_router: ^14.1.0 # Navigation
  bloc: ^8.1.4
  flutter_bloc: ^8.1.5

  # Added for Domain Layer & Data Models
  dartz: ^0.10.1
  equatable: ^2.0.5
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  dio: ^5.8.0+1
  pretty_dio_logger: ^1.4.0
  bloc_concurrency: ^0.2.5
  stream_transform: ^2.1.0
  shared_preferences: ^2.2.3
  # flutter_secure_storage: ^9.2.2 # Needed by CoreDioClient/Auth & AI Docs - Replaced by below
  package_info_plus: ^8.3.0

  # --- Add record and permission_handler ---
  # REMOVED record: ^5.0.5
  # REMOVED permission_handler: ^11.3.1 
  http: ^1.3.0
  carousel_slider: ^4.2.1
  smooth_page_indicator: ^1.1.0
  flutter_dotenv: ^5.1.0

  # Order Module specific / additions
  image_picker: ^1.1.2
  flutter_image_compress: ^2.3.0
  path_provider: ^2.1.3
  path: ^1.9.0
  file_picker: ^8.0.5

  # Drift dependencies (from order-module)
  drift: ^2.26.0
  sqlite3_flutter_libs: ^0.5.32

  # Audio/Record dependencies (from ai-docs/order-module)
  audioplayers: ^6.0.0
  record: ^5.0.5 # KEEP THIS ONE
  permission_handler: ^11.3.1 # KEEP THIS ONE

  # State Management (commented out) - Removed duplicate commented section

  # --- SVG Support ---
  flutter_svg: ^2.0.9 # Added for SVG Logo

  # --- Core Utilities ---
  flutter_secure_storage: ^9.2.2 # For secure storage (Token, UserID) - Kept this version
  connectivity_plus: ^6.0.3     # For network status checking

  # 网络和存储
  internet_connection_checker: ^1.0.0 # KEEP THIS ONE

  # UI和文件处理
  http_parser: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.7 # Add bloc_test back
  mockito: ^5.4.4 # Use Mockito for mocking
  flutter_lints: ^3.0.0 # Common, keep one entry

  # Code Generators
  build_runner: ^2.4.10 # Kept newer version from HEAD
  injectable_generator: ^2.6.1 # DI Code Generator
  riverpod_generator: ^2.4.0 # Riverpod Code Generator
  freezed: ^2.4.7 # From HEAD
  json_serializable: ^6.8.0 # Kept newer version from order-module
  drift_dev: ^2.26.0 # From order-module

  # Testing
  test: ^1.24.9 # From HEAD

  # Explicitly override analyzer version to resolve build_runner issues
  analyzer: ^6.4.1 # From HEAD

  # custom_lint: # Consider adding for custom lint rules later
  # riverpod_lint: # Consider adding specific Riverpod lint rules later

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    # - .env # Removed as .env is loaded at runtime, not packaged as an asset
    - assets/images/ # Declare the images directory
  #   - images/a_dot_burr.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
