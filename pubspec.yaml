name: dskk_flutter_refactor
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+202606162

environment:
  sdk: ">=3.4.0 <4.0.0" # Use higher constraint

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Often needed for internationalization
    sdk: flutter
  cupertino_icons: ^1.0.6 # Keep default icons

  # Core Libraries - Merged
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  get_it: ^7.7.0
  injectable: ^2.4.1
  go_router: ^14.1.0
  bloc: ^8.1.4
  flutter_bloc: ^8.1.5
  bloc_concurrency: ^0.2.5
  stream_transform: ^2.1.0

  # Domain/Data - Merged
  dartz: ^0.10.1
  equatable: ^2.0.5
  uuid: ^4.5.1 # 提现幂等 token 生成（原为传递依赖，提升为直接依赖）
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0

  # UI Helpers
  cached_network_image: ^3.3.1 # Added for image caching
  flutter_markdown: ^0.6.19 # 添加Markdown渲染支持
  url_launcher: ^6.2.5 # 用于处理Markdown链接点击
  webview_flutter: ^4.10.0 # 用于 Stripe 支付页面

  # Networking - Merged
  dio: ^5.8.0+1
  pretty_dio_logger: ^1.4.0
  dio_cache_interceptor: ^3.5.0
  dio_cache_interceptor_hive_store: ^3.2.2
  http: ^1.3.0
  http_parser: ^4.0.2
  web_socket_channel: ^3.0.3 # Updated for compatibility with drift

  # Storage - Merged
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.2
  drift: ^2.26.0
  sqlite3_flutter_libs: ^0.5.32

  # Utilities - Merged
  flutter_dotenv: ^5.1.0
  package_info_plus: ^8.3.0
  path_provider: ^2.1.3
  path: ^1.9.0
  connectivity_plus: ^6.0.3
  internet_connection_checker: ^1.0.0

  # Feature specific - Merged
  image_picker: ^1.1.2
  flutter_image_compress: ^2.3.0
  file_picker: ^8.0.5
  audioplayers: ^6.0.0
  record: ^5.0.5
  permission_handler: ^11.3.1
  flutter_staggered_grid_view: ^0.7.0
  flutter_svg: ^2.0.9
  
  # Chat UI
  flutter_chat_ui: ^1.6.14
  
  # File preview and download
  photo_view: ^0.15.0  # For image preview with zoom
  flutter_pdfview: ^1.3.2  # For PDF preview
  open_file: ^3.5.3  # For opening files with system apps

  # 支付功能
  # tobias: ^3.3.0 # 支付宝SDK Flutter插件 - 暂时禁用
  # fluwx: ^5.5.5 # 微信SDK Flutter插件 - 暂时禁用
  google_sign_in: ^6.2.1 # Google登录SDK
  fluttertoast: ^8.2.4 # 用于显示支付结果提示
  yaml: ^3.1.2 # 用于解析配置文件

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  bloc_test: ^9.1.7
  mockito: ^5.4.5
  flutter_lints: ^3.0.0

  # Code Generators - Merged
  build_runner: ^2.4.10
  injectable_generator: ^2.6.1
  riverpod_generator: ^2.4.0
  freezed: ^2.4.7
  json_serializable: ^6.8.0
  drift_dev: ^2.26.0

  intl_utils: ^2.8.8

dependency_overrides:
  analyzer: ^8.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/icons/nav/
    - assets/audio/
    - .env
    - .env.local-debug
    - .env.staging
    - config/
  #   - images/a_dot_burr.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
flutter_intl:
  enabled: true

# 支付宝 tobias 插件配置 - 暂时禁用
# tobias:
#   url_scheme: com.duoshaokankan.weapp
